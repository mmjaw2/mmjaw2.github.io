import { AbstractOperation, SerializableOperation } from './AbstractOperation';
export declare class Operation {
    static makeSerializable<T extends AbstractOperation>(op: T): SerializableOperation;
    static fromSerializable(serializable: SerializableOperation): AbstractOperation;
    static fillConstants<T extends AbstractOperation, TConstants extends Record<string, string | number>>(operation: T, constants: TConstants): T;
    static apply(src: string, operation: AbstractOperation): string;
}
